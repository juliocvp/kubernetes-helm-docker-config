apiVersion: apps/v1
kind: Deployment
metadata:
  name: practica-final-backend
  labels:
    app: spring-boot-app
    type: back-end
spec:
  template:
    metadata:
      name: practica-final-backend
      labels:
        app: spring-boot
        type: back-end
    spec:
      containers:
        - name: practica-final-backend
          image: "juliocvp/practica-final-backend:0.0.1-SNAPSHOT"
          imagePullPolicy: Always
  replicas: 1
  selector:
    matchLabels:
      app: spring-boot
      type: back-end
#---
#apiVersion: v1
#kind: Service
#metadata:
#  name: practica-final-backend
#spec:
#  selector:
#    app: spring-boot
#  type: NodePort
#  ports:
#    - protocol: TCP
#      targetPort: 8082
#      port: 8082
#      nodePort: 30001